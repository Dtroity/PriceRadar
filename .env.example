# JWT
JWT_ACCESS_SECRET=change-me-in-production
JWT_REFRESH_SECRET=change-me-refresh

# PostgreSQL
DATABASE_URL=postgres://postgres:password@localhost:5432/vizor360

# Redis (backend uses HOST/PORT, not URL)
REDIS_HOST=localhost
REDIS_PORT=6379

# App
FRONTEND_URL=http://localhost:5173
PORT=3001

# Google Cloud Vision (invoice OCR in backend worker)
# Host: path to service account JSON
GOOGLE_APPLICATION_CREDENTIALS=./keys/google.json
# Docker: compose sets GOOGLE_APPLICATION_CREDENTIALS=/app/keys/google.json via volume ./keys:/app/keys

# Yandex Foundation Models (invoice JSON via ai/client; без ключей — Ollama)
YANDEX_API_KEY=
YANDEX_FOLDER_ID=
# AI_MODEL=gpt-4o-mini  # имя модели для Ollama
OLLAMA_URL=http://localhost:11434

LOG_LEVEL=info

# Sentry (optional — omit to disable)
SENTRY_DSN=

# Telegram (optional)
TELEGRAM_BOT_ENABLED=false
TELEGRAM_BOT_TOKEN=

# Frontend (Vite): copy to frontend/.env.local when needed
# VITE_SENTRY_DSN=

# Grafana (docker compose) admin password
# GRAFANA_ADMIN_PASSWORD=admin
